# Always install exact versions (no ^ or ~ in package.json)
save-exact=true

# Use npm's main registry (you can swap for GitHub, GitLab, or Verdaccio if needed)
registry=https://registry.npmjs.org/

# If you use private npm packages, set up an auth token
# In CI (e.g., Vercel, GitHub Actions), inject NPM_TOKEN as an environment variable
//registry.npmjs.org/:_authToken=${NPM_TOKEN}

# Enforce strict SSL for security
strict-ssl=true

# Workspaces enabled (for monorepos/multiple packages)
workspaces=true

# Optional: quiet down npm logs during builds
loglevel=warn

# Fix peer dependency issues (useful for React/Next.js projects)
legacy-peer-deps=true

# Use pnpm for package management
package-manager-strict=true

# pnpm configuration
auto-install-peers=true
strict-peer-dependencies=false
